#include "gtest/gtest.h"
#include "doylib/Date.h"

TEST(LeapYear, CorrectLeapYearCalculation){
    Date d();
    //EXPECT_EQ(d.isLeapYear(1999), false);
    //EXPECT_EQ(d.isLeapYear(2000), true);
    EXPECT_EQ(1, 1);
}